# 给 Codex（以及任何进这个项目的 agent）

你不是第一个在这个项目里干活的 agent。这份文件是把已经踩过的坑写下来，
不是让你重新踩一遍。设计哲学（为什么止损要本地算、为什么期望值没验证够
样本就要老实说"尚未验证"）在 `README.md` 的"设计原则"那一节，先读那个。
这份文件只讲具体的、会让你写出错误代码的技术细节。

## 部署结构，先搞清楚再动手

```
本地（这台 Mac）   /Users/heliotrope/Projects/finance-agent   开发、改代码
线上（VPS）        root@179.255.101.244:/root/finance-agent   真实跑着的服务
                   systemd 服务名: finance-agent（8503 端口，nginx 反代到
                   invest.heliotrope.online）
```

改完代码要**手动同步到 VPS**（`scp` 或者 `git push` 之后到 VPS 上
`git pull`），本地改完不会自动生效。改完影响正在跑的服务的文件，记得
`systemctl restart finance-agent`。`.streamlit/secrets.toml`（API key 这些）
只在 VPS 上有，是 `.gitignore` 掉的，不要以为本地能读到它、也不要把它
的内容写死进代码里。

## AI 判断链路：四家供应商轮流顶，不是只有一家

`advisor.py` 里 `chat_with_failover` 现在挂着千问 → 智谱 → SiliconFlow →
方舟GLM-5.2 四家，任何一家挂了自动换下一家。**改这条链路时两件事必须
一起改**：
1. `_load_secrets_into_env()` 里的"是否已经加载过"判断要覆盖新供应商的
   key，漏了会导致新 key 永远读不进环境变量。
2. `_is_failover_worthy()` / `_is_quota_or_ratelimit_error()` 要认得住新
   供应商的错误码格式（今天真实踩过：402 余额不足没被识别成"该换下一家"，
   直接抛出，后面配置好的供应商根本没被调用到）。

`judge_watchlist()` 的并发数是 `7 × 健康供应商数`，不要写死成固定值——
只剩一家时 20 路并发会把它打垮，136 支候选池最后只有几十支跑出结果。

## 港股这几个字段，接口给的东西不是你以为的意思

- **`CURRENCY` 字段是"上市交易币种"，不是"财报币种"。** 港股一律显示
  HKD，但腾讯小米这类内地公司财报实际是人民币，友邦汇丰是美元。别直接
  拿这个字段的值当财报货币单位来用，要么老实说"数据源只给交易币种，
  不确定"，要么去查真实的报表货币。
- **`lot_size`（每手股数）没有统一值。** 实测同一批标的从 20 股
  （MiniMax）到 500 股（东方甄选）都有，写死成 100 会导致给出的建议
  股数在券商那边根本下不了单。用 `ds.get_hk_lot_size()`，别猜。
- **分析师一致预期接口（`get_research_analyst_consensus`）对薄覆盖的票
  会间歇性只返回一半字段。** 同一支票连续调用，有时候有 `total`
  （覆盖机构数），有时候没有。`total` 缺失不等于"0家覆盖"，这两者要
  分开处理，缺失时最多重试几次。

## 打分和目标价：这几个尺度不能混

- **止损和目标价不能用同一把尺子。** 止损衡量的是"日常噪音有多大"
  （ATR），目标价衡量的是"这个持有期内能走多远"，两者混用会让盈亏比
  变成一个跟标的无关的常数。目标价现在用"量度幅度"（突破前整理区间
  的高度往上投影），不是随手一个 ATR 倍数。
- **候选池的排序和截断顺序：先扩大候选池、让全部候选过盈亏比闸门，
  再从达标的里面挑；不要先按分数砍到展示条数、再对这一小撮过闸门。**
  打分奖励强势股，强势股恰好上方空间小、盈亏比差，先砍再筛会导致榜单
  常年只剩"分数高但赔率差"的票。
- **目标价相对现价的涨跌幅，代码直接算好喂给模型，别让模型自己算。**
  一天内模型把这个算错了三次（符号搞反、百分比算错）。
- **内部人交易数据里，只有写着"买入"两个字才是掏钱在公开市场买。**
  "其他获得""衍生品获得""授予"是薪酬发放，不构成看多信号。同一天
  好几个人拿到完全相同的股数，几乎可以确定是年度股权授予——这个可以
  写死成规则判断，不用留给模型去猜。

## 持仓 vs 自选，这两个字段容易搞反

`positions` 表里 `shares=0` 的行是"只关注不持仓"（自选），`shares>0`
才是真持仓。写筛选条件时如果只想要真持仓、用了 `if shares > 0`，别忘了
自选那批默认会被这个条件整个滤掉——这个 bug 在盘中盯盘功能上线以来
一直存在，21 支自选一次都没被监控过。

## 改完之后怎么验证

这个项目没有单元测试套件，验证方式是**跑一遍真实数据、查数据库**。
改了打分逻辑，跑 `advisor.py` 之后去 `data/track_record.db` 的 `advice`
表里核对 `score_fundamental` 这几个维度分列有没有正常写进去（不是只看
总分——总分正常不代表维度分解析对了，这个坑踩过两次）。改了 daily_plan
的仓位/止损逻辑，实际跑一遍 `daily_plan.py`，人工核对几支标的的买入区间、
止损、目标价数字讲不讲得通，不要只看它能不能跑通不报错。

## 跟 claude-manager 之间除了 squad 还有个更稳的通道

squad 经常掉线/认错工作目录（见 ai-squad-hub 项目里的踩坑记录）。VPS 上
`/root/.agent-relay/` 是备用通道，双方都能直接 SSH 读写文件，不依赖 squad
进程状态。给你的文件叫 `to-codex_<时间戳>.md`，你回的叫
`to-claude_<时间戳>.md`，写完30分钟自动删（有cron清理），别当持久存储用。
开新会话时，squad receive 之外，顺手看一眼这个目录里有没有属于你的新文件。
