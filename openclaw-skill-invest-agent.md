---
name: invest-agent
description: 用户的投研站项目（finance-agent）。问股票判断、持仓怎么办、今天该做什么、新股要不要打、宏观数据、AI模拟盘状况时使用。数据都在本机，不要去网上找。
---

# 投研站（invest-agent）

用户蒋天奇自建的投研系统，跑在这台 VPS 上。他的目标很明确：**系统给出判断，
他在汇丰证券手动同步下单，靠这个赚钱。** 所以你的回答要能直接支撑下单决定，
不要停在"数据是这样"。

网页：https://invest.heliotrope.online
代码：/root/finance-agent　数据库：/root/finance-agent/data/track_record.db

## 最重要的一条：数学期望还没验证完

2026-09-06 查过：此前所有已复核记录的"判断→复核"间隔只有 0.97~1.43 天，
而这套打分七成权重压在按季度起作用的基本面和估值上。用一天的价格波动去
检验它，量到的是噪音。所以历史上那些"高分表现反而差"的数字**既不能证明
系统有效，也不能证明无效**——只说明还没测过。

09-04 起窗口已改成 6.9 天，第一批可信数据 09-07 之后才有。

**在验证跑出结果之前，你介绍任何评分/动作时都要带上这个前提**，措辞是
"筛选线索"而不是"建议买入"。用户要的是靠这个赚钱，那就更不能拿一个没测过
期望的信号让他上真金白银。查当前状态：

```
cd /root/finance-agent && venv/bin/python3 expectancy.py
```

## 常用命令

所有命令都要先 `cd /root/finance-agent`，用 `venv/bin/python3` 而不是系统 python。

| 想知道什么 | 跑什么 |
|---|---|
| 今天该买什么、买多少（最常用） | `venv/bin/python3 daily_plan.py` |
| 持仓现在什么情况 | `venv/bin/python3 position_report.py --session hk-mid --dry-run` |
| 策略期望值验证到哪一步 | `venv/bin/python3 expectancy.py` |
| 模拟盘刚才干了什么 | `cat data/last_sim_agent_run.log` |
| 有没有成交没推送 | `venv/bin/python3 trade_alerts.py --list` |
| 新股打不打 | 读库表 `ipo_briefs`（别现跑，AI 生成很慢） |
| 宏观解读 | 读库表 `macro_briefs` |

`daily_plan.py` 是最有用的一个，它给的是能直接下单的东西：买多少股、
约多少人民币、止损价、目标价、盈亏比。仓位按"单笔最多亏本金2%"反推，
所以止损远的标的仓位自然小。

## 自动跑的时间表（北京时间，都会推到用户微信）

| 时间 | 做什么 |
|---|---|
| 06:30 | advisor 全市场判断，产出当天评分 |
| 07:25 | 港股新股简报 |
| **08:25** | **港股开盘前推操作清单** |
| 12:00 | 港股午盘持仓报告 |
| 16:10 | 港股收盘报告 + 收益结算 |
| 17:50 | 记录期望值验证进度 |
| **21:00** | **美股开盘前推操作清单** |
| 00:30 / 04:10 | 美股午盘 / 收盘报告 |

用户问"怎么没收到"时，先看 `/var/log/finance-agent-plan.log` 和
`/var/log/finance-agent-report.log`，再看 `crontab -l`。

## 用户的实际流程

系统给清单 → 他在**汇丰证券**手动下单 → 在网页持仓里同步记一笔 →
持仓报告持续跟踪 → AI 判断转卖出时推送提醒 → 他卖掉后在网页平仓。

资金规模 **1 万人民币**（2026-09-06 设定，效果好再加）。所以很多高价股
（美光一股 6800 元、泡泡玛特一手 1.3 万）会因为超过单笔 30% 上限而进不了
可执行清单——这时要说清楚是"仓位约束进不去"，不是"不该买"，两者意义相反。

## 查数据库（比跑脚本快，优先用）

```
sqlite3 /root/finance-agent/data/track_record.db "SQL"
```

关键表：
- `advice` — 每次AI判断。`source` 分 watchlist(观察池) / position(持仓) / screen(全市场)。
  `score` 总分，`score_fundamental` 等六列是分维度。`review_price` 非空表示已复核。
- `positions` — 持仓和自选。`shares>0` 是真持仓，`shares=0` 是只关注。
- `simulated_orders` — 模拟盘委托。`notified_at` 非空表示已推送微信。
- `ipo_briefs` / `macro_briefs` / `ipo_performance` — 预生成的简报，直接读。

## 回答用户时的规矩

**具体公司的事实必须来自查询，不能凭印象编。** 这条踩过坑：曾经有人问一只
不存在的股票，AI 编出了完整的业务描述、上市时间、营收增速和可比公司估值，
数字具体到用户没法分辨真假。查不到就说查不到。

**不给指令性结论。** 用户自己下单，你提供的是判断依据和执行参数（价位、
止损、波动率），不是"买"或"卖"。他问"该不该加仓"，给的是系统当前的评分、
分歧点和风险，让他自己定。

**港股没有"红鞋"机制。** 只有绿鞋（超额配售选择权，招股章程里写作"超額配股權"）
和回拨机制。用户问红鞋时直接说明没有这回事，不要顺着编。

**不要出现 emoji。** 这是整个项目的硬规则，聊天、代码、推送都不许有。

## 推送时效

用户明确说过"及时提醒我操作不能掉链子"。成交推送由 `trade_alert_push.py`
纯脚本负责（不经模型，避免开场白被当成推送内容发出去），失败不销账、
下一轮重试——宁可重复推一次也不能漏。

如果用户问"怎么没收到通知"，先跑 `trade_alerts.py --list` 看有没有积压，
再看 `openclaw cron list` 里推送任务的状态。

## 别做的事

- 不要碰 `sim_trader.py` 里的 `TrdEnv.SIMULATE`，那是模拟盘和真实资金之间
  的安全边界。
- 不要在 VPS 上 `git checkout` 覆盖本地改动，那可能是别人写的，先 stash。
- 独立脚本必须 `sys.stdout.flush()` 之后再 `os._exit(0)`——富途 SDK 的线程
  不是 daemon 线程，不强制退出会挂住，而 `os._exit` 会丢掉没刷新的输出。
